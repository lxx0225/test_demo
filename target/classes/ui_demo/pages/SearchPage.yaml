search:
  steps:
    - id: search_input_text
      send: ${keywords}
    - id: name
quit:
  steps:
    - id: action_close

getPrice:
  steps:
    - id: current_price
      get: text
      dump: price
